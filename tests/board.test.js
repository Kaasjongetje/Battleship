const board = require('../board.js');

test.only('Je kunt horizontaal plaatsen', () => {
    expect(3).toBe(3);
});

test('Je kunt niet buiten de map horizontaal plaatsen', () => {

});

test('Je kunt verticaal plaatsen', () => {
    
});

test('Je kunt niet buiten de map verticaal plaatsen', () => {
    
});